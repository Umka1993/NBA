import {combineReducers} from "@reduxjs/toolkit";
import setAuthDataReducer from '../../modules/authorization/authorizationSlise'

export const rootReducer = combineReducers({
    setAuthDataReducer
})